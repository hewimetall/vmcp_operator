# Profile: resurche

Acceptance bundle for research-oriented MCP upstreams.

Ready entries (fixtures land with Phase 5): Context7, Tavily, SerpAPI as RemoteHttp;
Yandex/Sourcegraph as managed HTTP proxy ContainerImage when images exist;
agent-lsp-real-inspect as RemoteHttp.

Blocked managed agent-lsp remains documented with `RuntimeUnavailable`.
